<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Curfew.App.ScheduleGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Spacing="8">
        <StackPanel Orientation="Horizontal" Spacing="14">
            <RadioButton x:Name="ToolAllow" Content="Allow" GroupName="tool" IsChecked="True" />
            <RadioButton x:Name="ToolBlock" Content="Block" GroupName="tool" />
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Canvas x:Name="LabelCanvas" Grid.Column="0" />
            <Canvas x:Name="GridCanvas" Grid.Column="1" Background="Transparent"
                    PointerPressed="OnPointerPressed"
                    PointerMoved="OnPointerMoved"
                    PointerReleased="OnPointerReleased"
                    PointerCanceled="OnPointerReleased" />
        </Grid>
    </StackPanel>
</UserControl>
